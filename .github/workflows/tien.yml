name: Windows VPS 6h

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Tải & cấu hình ngrok v3
        run: |
          Invoke-WebRequest https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip -OutFile ngrok.zip
          Expand-Archive ngrok.zip -DestinationPath ngrok
          .\ngrok\ngrok.exe config add-authtoken 29eh4vE8GamGOGE6YtQMlvLlZ4F_5LZpkhESEDUB7ZerGNCek

      - name: Tạo tài khoản RDP
        run: |
          net user tien123 Tien@12345 /add
          net localgroup administrators tien123 /add

      - name: Mở RDP port & bật dịch vụ
        run: |
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 1

      - name: Chạy ngrok TCP tunnel (background)
        run: |
          Start-Process .\ngrok\ngrok.exe "tcp 3389" -WindowStyle Hidden

      - name: Đợi ngrok khởi động
        run: Start-Sleep -Seconds 10

      - name: Lấy địa chỉ RDP từ ngrok
        run: |
          $tunnel = Invoke-RestMethod -Uri "http://127.0.0.1:4040/api/tunnels"
          $addr = $tunnel.tunnels[0].public_url -replace "tcp://",""
          echo "✅ VPS đã sẵn sàng!"
          echo "🌐 Kết nối RDP: $addr"
          echo "👤 User: tien123"
          echo "🔑 Pass: Tien@12345"
          echo ""
          echo "👉 Mở Remote Desktop (mstsc) trên máy và nhập địa chỉ ở trên để kết nối."

      - name: Giữ VPS online 6h
        run: Start-Sleep -Seconds 21600
